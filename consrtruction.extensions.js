module.exports = {
    build: function(room, spawn, countOfExtensions, limitOfExtensions) {
        // TODO: Fill limit of extensions near spawn
        // room.createConstructionSite(STRUCTURE_EXTENSION);
    }
}